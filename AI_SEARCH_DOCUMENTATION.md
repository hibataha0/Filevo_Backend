# 🔍 دليل البحث الذكي باستخدام AI

## نظرة عامة

تم إضافة نظام بحث ذكي متقدم يستخدم Ollama + Llama 3 للبحث في الملفات. النظام يبحث في:

- ✅ اسم الملف
- ✅ الوصف والوسوم
- ✅ محتوى الملف (النص المستخرج)
- ✅ البحث الدلالي باستخدام AI

---

## الميزات

### 1. استخراج النصوص تلقائياً

- **PDF**: استخراج النص من ملفات PDF
- **Word (DOCX)**: استخراج النص من ملفات Word
- **Excel**: استخراج النص من ملفات Excel
- **Text Files**: استخراج النص من الملفات النصية
- **Code Files**: استخراج النص من ملفات الكود (.js, .py, .java, إلخ)

### 2. توليد Embeddings

- يتم توليد embeddings تلقائياً لكل ملف بعد رفعه
- يستخدم Ollama + Llama 3 لتوليد embeddings

### 3. تلخيص الملفات

- يتم تلخيص الملفات الطويلة تلقائياً
- الملخص يظهر في نتائج البحث

### 4. البحث الذكي

- **بحث نصي**: يبحث في الاسم، الوصف، الوسوم، والمحتوى
- **بحث دلالي (AI)**: يبحث باستخدام embeddings للعثور على ملفات مشابهة حتى لو لم تكن الكلمات مطابقة

---

## المتطلبات

### 1. تثبيت Ollama

```bash
# Windows
winget install Ollama.Ollama

# أو من الموقع الرسمي
# https://ollama.com/download
```

### 2. تشغيل Ollama

```bash
ollama serve
```

### 3. تحميل Llama 3

```bash
ollama pull llama3
```

---

## APIs المتاحة

### 1. البحث الذكي الشامل

**POST** `/api/v1/search/smart`

البحث في كل شيء: اسم الملف، الوصف، الوسوم، محتوى الملف، والبحث الدلالي.

**Headers:**

```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

**Body:**

```json
{
  "query": "ملفات المشروع",
  "limit": 20,
  "minScore": 0.2,
  "category": "Documents"
}
```

**Parameters:**

- `query` (required): نص البحث
- `limit` (optional): عدد النتائج (افتراضي: 20)
- `minScore` (optional): الحد الأدنى للتشابه (افتراضي: 0.2)
- `category` (optional): تصفية حسب الفئة (افتراضي: null)

**Response:**

```json
{
  "message": "Smart search completed successfully",
  "query": "ملفات المشروع",
  "resultsCount": 5,
  "results": [
    {
      "type": "file",
      "_id": "...",
      "name": "project-plan.pdf",
      "category": "Documents",
      "size": 1024000,
      "summary": "ملخص الملف...",
      "description": "وصف الملف",
      "tags": ["مشروع", "مهم"],
      "relevanceScore": 0.85,
      "searchType": "ai",
      "hasExtractedText": true,
      "createdAt": "2024-01-15T10:00:00.000Z",
      "updatedAt": "2024-01-15T10:30:00.000Z"
    }
  ]
}
```

---

### 2. البحث في محتوى الملفات فقط

**POST** `/api/v1/search/content`

يبحث فقط في النص المستخرج من الملفات.

**Body:**

```json
{
  "query": "JavaScript",
  "limit": 20
}
```

**Response:**

```json
{
  "message": "Content search completed successfully",
  "query": "JavaScript",
  "resultsCount": 3,
  "results": [
    {
      "type": "file",
      "_id": "...",
      "name": "app.js",
      "category": "Code",
      "summary": "ملخص...",
      "extractedTextPreview": "const express = require('express');..."
    }
  ]
}
```

---

### 3. البحث في اسم الملف فقط

**POST** `/api/v1/search/filename`

يبحث فقط في اسم الملف.

**Body:**

```json
{
  "query": "report",
  "limit": 20
}
```

---

### 4. معالجة ملف

**POST** `/api/v1/search/process/:fileId`

معالجة ملف يدوياً: استخراج النص، توليد embedding، تلخيص.

**Example:**

```
POST /api/v1/search/process/507f1f77bcf86cd799439011
```

**Response:**

```json
{
  "message": "File processed successfully",
  "file": {
    "_id": "...",
    "name": "document.pdf",
    "isProcessed": true,
    "hasExtractedText": true,
    "hasEmbedding": true,
    "hasSummary": true,
    "extractedTextLength": 5000
  }
}
```

---

### 5. إعادة معالجة ملف

**POST** `/api/v1/search/reprocess/:fileId`

إعادة معالجة ملف إذا فشلت المعالجة السابقة.

---

## أمثلة الاستخدام

### مثال 1: البحث عن ملفات المشروع

```bash
POST /api/v1/search/smart
{
  "query": "ملفات المشروع",
  "limit": 10
}
```

### مثال 2: البحث في ملفات JavaScript

```bash
POST /api/v1/search/content
{
  "query": "function async await",
  "limit": 15
}
```

### مثال 3: البحث عن ملف معين بالاسم

```bash
POST /api/v1/search/filename
{
  "query": "report-2024"
}
```

---

## المعالجة التلقائية

عند رفع ملف جديد:

1. ✅ يتم استخراج النص تلقائياً (إذا كان الملف يدعم ذلك)
2. ✅ يتم توليد embedding تلقائياً
3. ✅ يتم تلخيص الملف تلقائياً (إذا كان النص طويلاً)

**ملاحظة:** المعالجة تتم في الخلفية ولا تؤثر على سرعة رفع الملف.

---

## إعدادات Ollama

في ملف `config.env`:

```env
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3
```

---

## أنواع الملفات المدعومة

### استخراج النص:

- ✅ PDF (.pdf)
- ✅ Word (.docx)
- ✅ Excel (.xlsx, .xls)
- ✅ Text (.txt)
- ✅ Code Files (.js, .jsx, .ts, .py, .java, .cpp, .html, .css, .json, .xml, .yaml, .md, .sql, .sh)

### البحث الدلالي:

- ✅ جميع الملفات التي تم معالجتها (لها embedding)

---

## نصائح الاستخدام

1. **البحث الذكي**: استخدم `/search/smart` للبحث الشامل
2. **البحث السريع**: استخدم `/search/filename` للبحث السريع في الأسماء
3. **البحث في المحتوى**: استخدم `/search/content` للبحث في محتوى الملفات
4. **المعالجة اليدوية**: استخدم `/search/process/:fileId` لمعالجة ملف قديم

---

## استكشاف الأخطاء

### المشكلة: لا تظهر نتائج AI

**الحل:**

1. تأكد أن Ollama يعمل: `ollama serve`
2. تأكد أن Llama 3 محمّل: `ollama pull llama3`
3. تحقق من إعدادات `config.env`

### المشكلة: فشل استخراج النص

**الحل:**

- بعض أنواع الملفات لا تدعم استخراج النص (مثل الصور، الفيديو)
- استخدم `/search/reprocess/:fileId` لإعادة المحاولة

### المشكلة: البحث بطيء

**الحل:**

- استخدم `limit` أصغر
- استخدم `/search/filename` للبحث السريع

---

## التحديثات المستقبلية

- [ ] دعم المزيد من أنواع الملفات
- [ ] تحسين سرعة البحث
- [ ] إضافة فلاتر متقدمة
- [ ] دعم البحث في المجلدات

---

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، راجع ملف `SYSTEM_DOCUMENTATION.md`.

