# 🔄 تحديث: إرسال كود التحقق تلقائياً عند تسجيل الدخول

## 🎯 الهدف
عند محاولة المستخدم تسجيل الدخول بحساب غير مفعّل، يتم إرسال كود التحقق تلقائياً إلى البريد الإلكتروني بدلاً من إرجاع رسالة خطأ فقط.

## 📋 التعديل المنفذ

### تعديل دالة `login` في `services/authService.js`

**قبل التعديل:**
- عند محاولة تسجيل الدخول بحساب غير مفعّل، يتم إرجاع رسالة خطأ فقط
- المستخدم يحتاج إلى طلب إعادة إرسال الكود يدوياً

**بعد التعديل:**
- عند محاولة تسجيل الدخول بحساب غير مفعّل، يتم:
  1. ✅ توليد كود تحقق جديد تلقائياً
  2. ✅ حفظ الكود في قاعدة البيانات
  3. ✅ إرسال الكود إلى البريد الإلكتروني تلقائياً
  4. ✅ إرجاع رسالة تخبر المستخدم أنه تم إرسال الكود

## 🔄 سير العمل الجديد

### 1. تسجيل الدخول بحساب غير مفعّل

```
POST /api/v1/auth/login
Body: { email, password }

Response (403):
{
  "success": false,
  "message": "يرجى تفعيل حسابك أولاً. تم إرسال كود التحقق إلى بريدك الإلكتروني",
  "email": "user@example.com",
  "requiresVerification": true
}
```

**ما يحدث:**
1. التحقق من صحة البريد الإلكتروني وكلمة المرور
2. التحقق من أن الحساب غير مفعّل (`emailVerified = false`)
3. توليد كود تحقق جديد (6 أرقام)
4. حفظ الكود المشفر في قاعدة البيانات
5. إرسال الكود إلى البريد الإلكتروني
6. إرجاع رسالة تخبر المستخدم أنه تم إرسال الكود

### 2. تسجيل الدخول بحساب مفعّل

```
POST /api/v1/auth/login
Body: { email, password }

Response (200):
{
  "data": { ...user },
  "token": "..."
}
```

**ما يحدث:**
- تسجيل الدخول بشكل طبيعي
- إرجاع token

## 📧 رسالة البريد الإلكتروني

عند محاولة تسجيل الدخول بحساب غير مفعّل، يتم إرسال رسالة تحتوي على:

```
مرحباً [اسم المستخدم],

تم محاولة تسجيل الدخول إلى حسابك في Filevo.

كود التحقق من البريد الإلكتروني الخاص بك هو:
[كود التحقق]

هذا الكود صالح لمدة 10 دقائق.

يرجى إدخال هذا الكود لتفعيل حسابك وتسجيل الدخول.

إذا لم تطلب هذا الكود، يمكنك تجاهل هذه الرسالة.

شكراً لك،
فريق Filevo
```

## ⚙️ الميزات

- ✅ إرسال تلقائي لكود التحقق عند محاولة تسجيل الدخول
- ✅ لا حاجة لطلب إعادة الإرسال يدوياً
- ✅ تجربة مستخدم أفضل
- ✅ كود جديد في كل محاولة تسجيل دخول
- ✅ مدة صلاحية 10 دقائق

## 🔍 مثال الاستخدام

### Frontend - تسجيل الدخول

```javascript
// محاولة تسجيل الدخول
const response = await fetch('/api/v1/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'user@example.com',
    password: 'password123'
  })
});

const data = await response.json();

if (data.requiresVerification) {
  // الحساب غير مفعّل - تم إرسال الكود تلقائياً
  // عرض صفحة إدخال كود التحقق
  showVerificationCodeInput(data.email);
} else {
  // تسجيل الدخول نجح
  localStorage.setItem('token', data.token);
  redirectToDashboard();
}
```

### Frontend - التحقق من الكود

```javascript
// بعد إدخال الكود
const verifyResponse = await fetch('/api/v1/auth/verifyEmail', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'user@example.com',
    verificationCode: '123456'
  })
});

const verifyData = await verifyResponse.json();
// حفظ token وتسجيل الدخول
localStorage.setItem('token', verifyData.token);
redirectToDashboard();
```

## ⚠️ ملاحظات مهمة

1. **كود جديد في كل محاولة:**
   - كل مرة يحاول المستخدم تسجيل الدخول بحساب غير مفعّل، يتم توليد كود جديد
   - الكود السابق يصبح غير صالح

2. **مدة الصلاحية:**
   - الكود صالح لمدة 10 دقائق فقط
   - بعد انتهاء الصلاحية، يحتاج المستخدم إلى محاولة تسجيل الدخول مرة أخرى

3. **معالجة الأخطاء:**
   - إذا فشل إرسال البريد الإلكتروني، يتم حذف الكود وإرجاع خطأ
   - المستخدم يحتاج إلى المحاولة مرة أخرى

4. **الأمان:**
   - الكود مشفر باستخدام SHA-256
   - لا يتم إرجاع الكود في الاستجابة

## ✅ التحقق من التعديلات

- ✅ تعديل دالة login
- ✅ إرسال كود التحقق تلقائياً
- ✅ معالجة الأخطاء
- ✅ لا توجد أخطاء في linter

## 📄 الملفات المعدلة

1. ✅ `services/authService.js` - تعديل دالة `login`

## 🎉 النتيجة

الآن عند محاولة المستخدم تسجيل الدخول بحساب غير مفعّل:
- ✅ يتم إرسال كود التحقق تلقائياً إلى البريد الإلكتروني
- ✅ لا حاجة لطلب إعادة الإرسال يدوياً
- ✅ تجربة مستخدم أفضل وأسهل













