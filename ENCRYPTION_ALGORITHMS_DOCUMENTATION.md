# 🔐 توثيق خوارزميات التشفير المستخدمة في المشروع

## 📋 نظرة عامة

يستخدم المشروع عدة خوارزميات تشفير وأمان لحماية البيانات الحساسة:

1. **bcrypt** - لتشفير كلمات المرور
2. **JWT (JSON Web Tokens)** - للمصادقة والتفويض
3. **SHA-256** - لتشفير أكواد التحقق
4. **crypto.randomUUID()** - لتوليد معرفات فريدة

---

## 🔑 1. bcrypt - تشفير كلمات المرور

### الاستخدام
تستخدم خوارزمية **bcrypt** لتشفير كلمات المرور في المشروع.

### المواقع المستخدمة فيها:

#### أ) كلمات مرور المستخدمين (`models/userModel.js`)
```javascript
const bcrypt = require("bcryptjs");

// Pre-save hook - تشفير تلقائي قبل الحفظ
userSchema.pre("save", async function (next) {
  if (!this.isModified("password")) return next();
  this.password = await bcrypt.hash(this.password, 12); // ✅ 12 salt rounds
  next();
});
```

**التفاصيل:**
- **Salt Rounds**: 12 (مستوى أمان عالي)
- **التطبيق**: تلقائي عند إنشاء أو تحديث كلمة المرور
- **الموقع**: `models/userModel.js` - السطر 53

#### ب) تغيير كلمة المرور (`services/userService.js`)
```javascript
// عند تغيير كلمة المرور يدوياً
user.password = await bcrypt.hash(req.body.password, 12);
user.passwordChangedAt = Date.now();
```

**الموقع**: `services/userService.js` - السطر 60

#### ج) كلمات مرور المجلدات المحمية (`services/folderService.js`)
```javascript
// حماية المجلد بكلمة مرور
const passwordHash = await bcrypt.hash(password, 12);
folder.passwordHash = passwordHash;
```

**الموقع**: `services/folderService.js` - السطر 2240

### التحقق من كلمات المرور

#### أ) تسجيل الدخول (`services/authService.js`)
```javascript
// مقارنة كلمة المرور المدخلة مع المشفرة
if (!user || !(await bcrypt.compare(req.body.password, user.password))) {
  return next(new ApiError("Incorrect email or password", 401));
}
```

**الموقع**: `services/authService.js` - السطر 36

#### ب) التحقق من كلمة مرور المجلد (`services/folderService.js`)
```javascript
// التحقق من كلمة مرور المجلد المحمي
const isPasswordValid = await bcrypt.compare(password, folder.passwordHash);
if (!isPasswordValid) {
  return next(new ApiError("Access denied. Invalid password", 403));
}
```

**الموقع**: `services/folderService.js` - السطر 2301

### معلومات تقنية عن bcrypt

| الخاصية | القيمة |
|---------|--------|
| **الخوارزمية** | bcrypt (Blowfish-based) |
| **Salt Rounds** | 12 |
| **مستوى الأمان** | عالي جداً |
| **الوقت المقدر للتشفير** | ~300ms لكل كلمة مرور |
| **المقاومة** | مقاومة لـ Brute Force و Rainbow Tables |
| **التوافق** | متوافق مع جميع أنظمة التشغيل |

**ملاحظات:**
- ✅ **Salt Rounds = 12**: يوفر توازن جيد بين الأمان والأداء
- ✅ **Adaptive Hashing**: bcrypt يتكيف مع قوة الحاسوب
- ✅ **One-way Hashing**: لا يمكن فك التشفير (irreversible)

---

## 🎫 2. JWT (JSON Web Tokens) - المصادقة

### الاستخدام
تستخدم **JWT** لإنشاء tokens للمصادقة والتفويض.

### إنشاء Token (`utils/createToken.js`)
```javascript
const jwt = require('jsonwebtoken');

const createToken = (payload) =>
  jwt.sign(
    { userId: payload },                    // Payload (البيانات)
    process.env.JWT_SECRET_KEY,            // Secret Key
    { expiresIn: process.env.JWT_EXPIRES_IN } // مدة الصلاحية
  );
```

**التفاصيل:**
- **Algorithm**: HS256 (افتراضي)
- **Payload**: `{ userId: ObjectId }`
- **Secret Key**: من متغيرات البيئة `JWT_SECRET_KEY`
- **Expiration**: من متغيرات البيئة `JWT_EXPIRES_IN` (افتراضي: 90 يوم)

**الموقع**: `utils/createToken.js` - السطر 4-6

### التحقق من Token (`middlewares/verifyToken.js`)
```javascript
// 1) استخراج Token من Header
if (req.headers.authorization && req.headers.authorization.startsWith("Bearer")) {
  token = req.headers.authorization.split(" ")[1];
}

// 2) التحقق من Token
const decoded = jwt.verify(token, process.env.JWT_SECRET_KEY);

// 3) التحقق من وجود المستخدم
const currentUser = await User.findById(decoded.userId);

// 4) التحقق من تغيير كلمة المرور بعد إصدار Token
if (currentUser.passwordChangedAt) {
  const passChangedTimestamp = parseInt(
    currentUser.passwordChangedAt.getTime() / 1000, 10
  );
  if (passChangedTimestamp > decoded.iat) {
    return next(new ApiError("User recently changed password. Please log in again.", 401));
  }
}
```

**الموقع**: `middlewares/verifyToken.js` - السطر 25

### استخدام JWT في Socket.IO (`socket.js`)
```javascript
// التحقق من Token في Socket.IO connections
const decoded = jwt.verify(token, process.env.JWT_SECRET_KEY);
```

**الموقع**: `socket.js` - السطر 35

### معلومات تقنية عن JWT

| الخاصية | القيمة |
|---------|--------|
| **الخوارزمية** | HS256 (HMAC-SHA256) |
| **النوع** | Signed Token |
| **البنية** | Header.Payload.Signature |
| **مدة الصلاحية** | 90 يوم (قابل للتعديل) |
| **الاستخدام** | المصادقة والتفويض |

**مكونات JWT:**
1. **Header**: نوع الـ token والخوارزمية
2. **Payload**: البيانات (userId)
3. **Signature**: التوقيع الرقمي

**ميزات:**
- ✅ **Stateless**: لا يحتاج تخزين في السيرفر
- ✅ **Scalable**: يعمل مع عدة سيرفرات
- ✅ **Secure**: محمي بالتوقيع الرقمي
- ✅ **Expiration**: تنتهي صلاحيته تلقائياً

---

## 🔒 3. SHA-256 - تشفير أكواد التحقق

### الاستخدام
تستخدم **SHA-256** لتشفير أكواد التحقق (Password Reset Codes, Email Change Codes).

### أ) أكواد إعادة تعيين كلمة المرور (`services/authService.js`)

#### إنشاء الكود المشفر:
```javascript
const crypto = require("crypto");

// 1) توليد كود عشوائي (6 أرقام)
const resetCode = Math.floor(100000 + Math.random() * 900000).toString();

// 2) تشفير الكود باستخدام SHA-256
const hashedResetCode = crypto
  .createHash("sha256")
  .update(resetCode)
  .digest("hex");

// 3) حفظ الكود المشفر في قاعدة البيانات
user.passwordResetCode = hashedResetCode;
user.passwordResetExpires = Date.now() + 10 * 60 * 1000; // 10 دقائق
```

**الموقع**: `services/authService.js` - السطر 126-132

#### التحقق من الكود:
```javascript
// 1) تشفير الكود المدخل
const hashedResetCode = crypto
  .createHash("sha256")
  .update(req.body.resetCode)
  .digest("hex");

// 2) البحث عن المستخدم بالكود المشفر
const user = await User.findOne({
  passwordResetCode: hashedResetCode,
  passwordResetExpires: { $gt: Date.now() }
});
```

**الموقع**: `services/authService.js` - السطر 166-174

### ب) أكواد تغيير البريد الإلكتروني (`services/userService.js`)

#### إنشاء الكود المشفر:
```javascript
const crypto = require("crypto");

// توليد كود عشوائي (6 أرقام)
const changeCode = Math.floor(100000 + Math.random() * 900000).toString();

// تشفير الكود
const hashedCode = crypto
  .createHash("sha256")
  .update(changeCode)
  .digest("hex");

// حفظ الكود المشفر
user.emailChangeCode = hashedCode;
user.emailChangeExpires = Date.now() + 10 * 60 * 1000; // 10 دقائق
```

**الموقع**: `services/userService.js` - السطر 109-116

#### التحقق من الكود:
```javascript
// تشفير الكود المدخل ومقارنته
const hashedCode = crypto
  .createHash("sha256")
  .update(req.body.verificationCode)
  .digest("hex");

if (user.emailChangeCode !== hashedCode) {
  return next(new ApiError("Invalid verification code", 400));
}
```

**الموقع**: `services/userService.js` - السطر 214-219

### معلومات تقنية عن SHA-256

| الخاصية | القيمة |
|---------|--------|
| **الخوارزمية** | SHA-256 (Secure Hash Algorithm 256-bit) |
| **النوع** | Cryptographic Hash Function |
| **مستوى الأمان** | عالي |
| **الطول** | 64 حرف hex (256 bit) |
| **الاستخدام** | تشفير أكواد التحقق |
| **One-way** | لا يمكن فك التشفير |

**ميزات:**
- ✅ **Deterministic**: نفس المدخل يعطي نفس المخرجات
- ✅ **Fast**: سريع جداً
- ✅ **Collision Resistant**: صعب جداً إيجاد مدخلين بنفس الـ hash
- ✅ **One-way**: لا يمكن فك التشفير

**ملاحظات:**
- ⚠️ **لا يُستخدم لحماية كلمات المرور**: SHA-256 سريع جداً وليس مناسباً لكلمات المرور
- ✅ **مثالي لأكواد التحقق**: سريع وآمن لأكواد قصيرة المدى

---

## 🆔 4. crypto.randomUUID() - توليد معرفات فريدة

### الاستخدام
تستخدم **crypto.randomUUID()** لتوليد أسماء ملفات فريدة.

### مثال (`middlewares/userImageMiddleware.js`)
```javascript
const crypto = require("crypto");

// توليد اسم فريد لصورة الملف الشخصي
const filename = `user-profile-${crypto.randomUUID()}-${Date.now()}.jpeg`;
```

**الموقع**: `middlewares/userImageMiddleware.js` - السطر 13

### معلومات تقنية

| الخاصية | القيمة |
|---------|--------|
| **الخوارزمية** | UUID v4 (Random) |
| **الطول** | 36 حرف (مع الشرطات) |
| **التنسيق** | xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx |
| **الاستخدام** | أسماء ملفات فريدة |

**ميزات:**
- ✅ **Unique**: احتمالية التكرار شبه مستحيلة
- ✅ **Random**: عشوائي تماماً
- ✅ **Standard**: معيار UUID v4

---

## 📊 جدول مقارنة الخوارزميات

| الخوارزمية | الاستخدام | الأمان | السرعة | قابلية الفك |
|------------|-----------|--------|--------|-------------|
| **bcrypt** | كلمات المرور | ⭐⭐⭐⭐⭐ | ⭐⭐ | ❌ لا |
| **JWT (HS256)** | Tokens | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ (للتحقق فقط) |
| **SHA-256** | أكواد التحقق | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ لا |
| **UUID v4** | معرفات فريدة | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | - |

---

## 🔐 أفضل الممارسات المطبقة

### ✅ 1. كلمات المرور
- ✅ استخدام bcrypt مع 12 salt rounds
- ✅ لا يتم إرجاع كلمات المرور في أي response
- ✅ التحقق من كلمة المرور القديمة قبل التغيير
- ✅ تسجيل `passwordChangedAt` لإبطال Tokens القديمة

### ✅ 2. JWT Tokens
- ✅ Secret Key قوي ومخزن في متغيرات البيئة
- ✅ مدة صلاحية محددة (90 يوم)
- ✅ التحقق من تغيير كلمة المرور بعد إصدار Token
- ✅ التحقق من وجود المستخدم قبل السماح بالوصول

### ✅ 3. أكواد التحقق
- ✅ تشفير SHA-256 قبل الحفظ
- ✅ مدة صلاحية محددة (10 دقائق)
- ✅ إرسال الكود الأصلي (غير مشفر) عبر الإيميل فقط
- ✅ حذف الكود بعد الاستخدام

### ✅ 4. المجلدات المحمية
- ✅ كلمات المرور مشفرة بـ bcrypt
- ✅ `passwordHash` لا يتم إرجاعه في responses (`select: false`)
- ✅ التحقق من الصلاحيات قبل الوصول

---

## 🛡️ الأمان الإضافي

### 1. حماية من NoSQL Injection
```javascript
// express-mongo-sanitize
app.use(mongoSanitize());
```

### 2. حماية من XSS
```javascript
// xss-clean
app.use(xss());
```

### 3. Rate Limiting
```javascript
// حماية من Brute Force
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 دقيقة
  max: 100 // 100 طلب لكل IP
});
```

### 4. Helmet
```javascript
// حماية HTTP headers
app.use(helmet());
```

---

## 📝 ملاحظات مهمة

### ⚠️ ما يجب تجنبه:

1. **لا تستخدم SHA-256 لكلمات المرور**
   - SHA-256 سريع جداً وليس مناسباً
   - استخدم bcrypt دائماً

2. **لا تخزن Secret Keys في الكود**
   - استخدم متغيرات البيئة دائماً
   - لا ترفع `.env` إلى Git

3. **لا ترجع كلمات المرور في Responses**
   - حتى المشفرة
   - استخدم `select: false` في Mongoose

4. **لا تستخدم Tokens منتهية الصلاحية**
   - تحقق من `exp` في JWT
   - أعد تسجيل الدخول عند انتهاء الصلاحية

### ✅ ما يجب فعله:

1. **استخدم bcrypt دائماً لكلمات المرور**
   - Salt rounds = 12 أو أكثر
   - لا تقل عن 10

2. **استخدم JWT للمصادقة**
   - Secret Key قوي (32+ حرف)
   - مدة صلاحية معقولة

3. **شفر أكواد التحقق**
   - استخدم SHA-256
   - مدة صلاحية قصيرة (10 دقائق)

4. **تحقق من الصلاحيات**
   - تحقق من Token في كل request محمي
   - تحقق من ملكية المورد

---

## 🔄 سير العمل (Workflows)

### 1. تسجيل الدخول
```
1. المستخدم يدخل email + password
2. البحث عن المستخدم بالإيميل
3. bcrypt.compare(password, user.password)
4. إذا صحيح → إنشاء JWT token
5. إرجاع Token + بيانات المستخدم
```

### 2. إعادة تعيين كلمة المرور
```
1. المستخدم يطلب إعادة تعيين
2. توليد كود عشوائي (6 أرقام)
3. SHA-256 hash للكود
4. حفظ الكود المشفر في DB
5. إرسال الكود الأصلي (غير مشفر) عبر الإيميل
6. المستخدم يدخل الكود
7. SHA-256 hash للكود المدخل
8. مقارنة مع الكود المشفر في DB
9. إذا صحيح → السماح بتغيير كلمة المرور
10. bcrypt.hash للكلمة الجديدة
11. حفظ الكلمة المشفرة
```

### 3. حماية المجلد
```
1. المستخدم يضع كلمة مرور للمجلد
2. bcrypt.hash(password, 12)
3. حفظ passwordHash في DB
4. عند الوصول للمجلد:
   - طلب كلمة المرور
   - bcrypt.compare(password, folder.passwordHash)
   - إذا صحيح → السماح بالوصول
```

---

## 📚 المراجع

- **bcrypt**: https://www.npmjs.com/package/bcryptjs
- **JWT**: https://jwt.io/
- **SHA-256**: https://en.wikipedia.org/wiki/SHA-2
- **UUID**: https://en.wikipedia.org/wiki/Universally_unique_identifier

---

## ✅ الخلاصة

المشروع يستخدم **4 خوارزميات تشفير رئيسية**:

1. **bcrypt** (12 rounds) - كلمات المرور ✅
2. **JWT (HS256)** - المصادقة ✅
3. **SHA-256** - أكواد التحقق ✅
4. **UUID v4** - معرفات فريدة ✅

جميع الخوارزميات مطبقة بشكل صحيح وتتبع أفضل الممارسات الأمنية.

---

**تاريخ الإنشاء**: 2024  
**آخر تحديث**: 2024
